class ArtistCard {
  String cardTitle;
  String cardImage;
  double topMargin;

  ArtistCard(String title, String imagePath, double marginTop) {
    cardTitle = title;
    cardImage = imagePath;
    topMargin = marginTop;
  }
}